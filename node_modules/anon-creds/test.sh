mkdir tmp
mkdir tmp/schema
mkdir tmp/app

APP_DIR="$PWD/tmp/app"
SCHEMA_DIR="$PWD/tmp/schema"

node schema-generator 1000 "$PWD/tmp"
schema=$SCHEMA_DIR/*
apps=$APP_DIR/*

for app in $apps
do
  cat $app
done

rm -rf tmp
